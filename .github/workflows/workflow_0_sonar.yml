name: workflow_0_sonar
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sonarcube:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0

    # Triggering SonarQube analysis as results of it are required by Quality Gate check.   
    #- name: SonarCube Scan
    #  uses: sonarsource/sonarqube-scan-action@master
    #  env:
    #    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #    SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

    - name: SonarQube Scan
      uses: sonarsource/sonarcloud-github-action@master
        with:
          token: ${{ secrets.SONAR_TOKEN }}
          organization: "TuneDemo"
          projectKey: "tunedemo-1"
          projectName: "tips_claim_service_github_actions"
          projectBaseDir: "./"
          extraProperties: |
            sonar.host.url=https://sonarcloud.io/summary/new_code?id=TuneDemo_tips_claim_service_github_actions
        

    # Check the Quality Gate status.
    - name: SonarQube Quality Gate check
      id: sonarqube-quality-gate-check
      uses: sonarsource/sonarqube-quality-gate-action@master
      
    # Force to fail step after specific time.
      timeout-minutes: 3
      env:
       SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
       SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }} #OPTIONAL

    # The possible outputs of the `quality-gate-status` variable are `PASSED`, `WARN` or `FAILED`.
    - name: "Example show SonarQube Quality Gate Status value"
      run: echo "The Quality Gate status is ${{ steps.sonarqube-quality-gate-check.outputs.quality-gate-status }}"       
